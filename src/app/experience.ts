export interface Experience {
    titre: string;
    presentation?: string[];
    presentation_details?: Record<number, string[]>;
    titre_liste?: string;
    faits: string[];
    image?: string;
}
